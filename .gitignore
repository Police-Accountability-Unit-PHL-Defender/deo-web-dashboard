# Nuxt dev/build outputs
.output
.nuxt

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store

# Local env files
.env
.env.*
!.env.example
.envrc

# e2e parity harness artifacts (rendered page text)
e2e/.artifacts/

# Legacy build output symlink
dist

# Pipeline inputs: multi-hundred-MB backups and the SQLite built from them.
# Patterns, not the directory — pipeline/data/demographics/ is tracked and is
# the only record of where districts.json's numbers come from.
pipeline/data/*.zip
pipeline/data/*.db

# uv virtual environment for the pipeline project
pipeline/.venv
